#pragma once 

#include <vector>
#include <string>
#include <unordered_map>

namespace cppbits {
    namespace utils {
        namespace str {

            __declspec(dllexport)
            std::string printVector(std::vector<std::string> vec);
        }
    }
}
